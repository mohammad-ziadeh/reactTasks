import React from "react";

export default function task4({ userName, email, age }) {
  return (
    <div>
      <p>UserName: {userName}</p>
      <p>Email: {email}</p>
      <p>Age: {age}</p>
    </div>
  );
}
